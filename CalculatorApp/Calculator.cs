public enum SIGN
{
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE
}

public class Calculator
{
    public double Calculate(SIGN sign, double arg1, double arg2)
    {
        switch (sign)
        {
           

            default:
                return 0;
        }
    }
}
